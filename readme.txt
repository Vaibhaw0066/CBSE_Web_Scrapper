Preffered python version == 3.8

1. Please install all the dependencies before running the script to avoid "Module not found error". 
	a. Navigate to cbse folder
	b. fire up cmd prompt
	c. run pip3 install -r requirements.txt command to intall all the dependencies at once.

2. Mention the full path of *chromedriver* with .exe extension. 